
import { Given, When, Then } from "@cucumber/cucumber";
import { expect } from "@playwright/test";
import loginData from "../../../test-data/loginData.json";

       
         When('when the user clicks on the Add Course Structure', async function () {
          
         });
       
 
         When('The user clicks on the Similar Courses', async function () {
           
         });
       

       
         When('Select the Course {string} and select the {string} to be duplicated', async function (string, string2) {
           
         });
 
         When('Select the Available Hierarchy Levels and Select Module Rows to duplicate', async function () {
          
         });

       
         When('Clicks on Duplicate btn  the Course', async function () {
           
         });
       
 
       
         Then('The Course Structure is Added Successfully', async function () {
          
         });
       
  
